# Frontend Gotita Roja

Si vas a probar en local, usa `http://localhost:3000/api` como backend.

Si lo publicas en GitHub Pages, edita `config.js` o usa el parámetro `?api=https://tu-backend/api` para apuntar a tu backend público.
